```bash
sudo nixos-rebuild switch --flake ~/nix-configs#nixos

sudo nixos-rebuild test --flake ~/nix-configs#nixos

home-manager switch --flake ~/nix-configs#astro@nixos

nix flake show ~/nix-configs
```
